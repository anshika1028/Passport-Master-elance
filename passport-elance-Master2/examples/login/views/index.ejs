<% if (!user) { %>
	<h2>Welcome! Please log in.</h2>
	<p>
			<a href="/">Home</a> | 
			<a href="/login">Log In</a>
	</p>
<% } else { %>
	<h2>User ID, <%= user.id %>.</h2>
	<h2>User NAme, <%= user.name %>.</h2>
	<h2>Hello, <%= user.id %>.</h2>
<% } %>
